import useStore from './Store';
import { TextStyle } from 'react-native';

export const useGetStyles = () => {
  const { mode } = useStore();

  const styles = {
    headerStyle: {
      backgroundColor: mode === 'dark' ? '#212529' : '#f8f9fa',
      elevation: 0,
      shadowOpacity: 0,
      borderBottomWidth: 0,
      height: 40,
      paddingVertical: 0,
      borderRadius: 15,
    },
    headerTintColor: mode === 'dark' ? '#fff' : '#212529',
    headerTitleStyle: {
      fontWeight: 'bold' as 'bold',
      fontSize: 18,
    },
    headerTitleAlign: 'center' as 'center',
    container: {
      flex: 1,
      padding: 20,
      backgroundColor: mode === 'dark' ? '#212529' : '#fff',
      borderRadius: 10,
    },
    contentContainer: {
      alignItems: 'center' as 'center' | 'flex-start',
      justifyContent: 'center' as 'center' | 'flex-start',
    },
    menuItem: {
      flexDirection: 'row' as 'row',
      alignItems: 'center' as 'center' | 'flex-start',
      padding: 15,
      marginVertical: 10,
      backgroundColor: '#005eb8' as '#005eb8',
      width: '80%' as '80%',
      borderRadius: 5,
    },
    menuIcon: {
      marginRight: 10,
    },
    menuText: {
      color: '#fff' as '#fff',
      fontSize: 18,
      fontWeight: 'bold' as 'bold',
      textAlign: 'center' as 'center' | 'left',
    },
    title: {
      fontSize: 24,
      fontWeight: 'bold' as 'bold',
      marginBottom: 20,
      color: mode === 'dark' ? '#fff' : '#212529',
    },
    radioContainer: {
      width: '100%' as '100%',
    },
    radioItem: {
      flexDirection: 'row' as 'row',
      alignItems: 'center' as 'center' | 'flex-start',
      marginVertical: 10,
    },
    radioText: {
      fontSize: 18,
      color: mode === 'dark' ? '#fff' : '#212529',
    },
    cartItem: {
      flex: 1,
      padding: 10,
      backgroundColor: mode === 'dark' ? '#fff' : '#212529',
      borderRadius: 10,
    },
    cartItemImage: {
      width: 100,
      height: 100,
      borderRadius: 10,
    },
    cartItemDetail: {
      flex: 1,
      padding: 10,
      backgroundColor: mode === 'dark' ? '#fff' : '#212529',
      borderRadius: 10,
    },
    cartSummary: {
      flex: 1,
      padding: 10,
      backgroundColor: mode === 'dark' ? '#fff' : '#212529',
      borderRadius: 10,
    },
    button: {
      padding: 10,
      margin: 5,
      backgroundColor: '#005eb8' as '#005eb8',
      borderRadius: 10,
    },
    redButton: {
      padding: 10,
      margin: 5,
      backgroundColor: '#ff0000' as '#ff0000',
      borderRadius: 10,
    },
    input: {
      padding: 10,
      margin: 5,
      borderColor: mode === 'dark' ? '#005eb8' : '#212529',
      borderWidth: 1,
      backgroundColor: mode === 'dark' ? '#212529' : '#fff',
      color: mode === 'dark' ? '#fff' : '#212529',
      borderRadius: 10,
      fontSize: 16,
      fontWeight: '500' as '500',
      shadowColor: '#000' as '#000',
      shadowOffset: { width: 0, height: 2 },
      shadowOpacity: 0.1,
      shadowRadius: 1.5,
      elevation: 2,
      width: '100%' as '100%',
    },
    label: {
      fontSize: 16,
      fontWeight: 'bold' as 'bold',
      color: mode === 'dark' ? '#212529' : '#fff',
    },
    activityIndicator: {
      margin: 5,
    },
    error: {
      color: 'red' as TextStyle['color'],
      fontSize: 16,
      textAlign: 'center' as 'center' | 'left',
    },
    category: {
      padding: 10,
      margin: 5,
      backgroundColor: mode === 'dark' ? '#fff' : '#212529',
      borderRadius: 10,
    },
    filterItem: {
      flex: 1,
      flexDirection: 'row' as 'row',
      alignItems: 'center' as 'center' | 'flex-start',
      padding: 10,
      margin: 5,
      backgroundColor: mode === 'dark' ? '#212529' : '#fff',
      borderRadius: 10,
      borderWidth: 1,
    },
    filterText: {
      flex: 1,
      marginLeft: 5,
      color: mode === 'dark' ? '#fff' : '#212529',
      fontSize: 16,
    },
    material: {
      padding: 10,
      margin: 5,
      backgroundColor: mode === 'dark' ? '#fff' : '#212529',
      borderRadius: 10,
    },
    productItem: {
      padding: 10,
      margin: 5,
      backgroundColor: mode === 'dark' ? '#fff' : '#212529',
      borderRadius: 10,
    },
    noResults: {
      fontSize: 16,
      fontWeight: 'bold' as 'bold',
      color: mode === 'dark' ? '#212529' : '#fff',
    },
    enhancedContainer: {
      flex: 1,
      padding: 16,
      backgroundColor: mode === 'dark' ? '#fff' : '#212529',
      borderRadius: 10,
    },
    filterContainer: {
      flex: 1,
      padding: 16,
      backgroundColor: mode === 'dark' ? '#212529' : '#fff',
      borderRadius: 10,
    },
    card: {
      borderWidth: 1,
      borderColor: '#005eb8' as '#005eb8',
      borderRadius: 10,
      overflow: 'hidden' as 'hidden',
      margin: 10,
      backgroundColor: mode === 'dark' ? '#212529' : '#fff',
    },
    orderCard: {
      borderWidth: 1,
      borderColor: '#005eb8' as '#005eb8',
      borderRadius: 10,
      overflow: 'hidden' as 'hidden',
      margin: 10,
      backgroundColor: mode === 'dark' ? '#212529' : '#fff',
      padding: 10,
    },
    activeCard: {
      borderColor: 'green' as 'green',
    },
    cardTitle: {
      fontSize: 18,
      fontWeight: 'bold' as 'bold',
      color: mode === 'dark' ? '#fff' : '#212529',
    },
    cardText: {
      fontSize: 16,
      color: mode === 'dark' ? '#fff' : '#212529',
    },
    buttonText: {
      color: '#fff' as '#fff',
      textAlign: 'center' as 'center' | 'left',
    },
    productImage: {
      width: '100%' as '100%',
      height: '100%' as '100%',
      resizeMode: 'cover' as 'cover',
      margin: 0,
      padding: 0,
      borderRadius: 10,
    },
    cardBody: {
      padding: 10,
    },
    buttonDisabled: {
      backgroundColor: '#aaa' as '#aaa',
      padding: 10,
      marginTop: 10,
      borderRadius: 10,
    },
    image: {
      width: '100%' as '100%',
      height: 200,
      resizeMode: 'cover' as 'cover',
    },
    grayscaleImage: {
      width: '100%' as '100%',
      height: 200,
      resizeMode: 'cover' as 'cover',
      tintColor: 'gray' as 'gray',
    },
    categoryImage: {
      width: '100%' as '100%',
      height: 200,
      resizeMode: 'cover' as 'cover',
    },
    text: {
      color: mode === 'dark' ? '#fff' : '#212529',
      fontSize: 16,
    },
    priceText: {
      color: mode === 'dark' ? '#fff' : '#212529',
      fontSize: 18,
      fontWeight: 'bold' as 'bold',
    },
    outOfStockButton: {
      backgroundColor: '#aaa' as '#aaa',
      padding: 10,
      borderRadius: 10,
    },
    addToCartButton: {
      backgroundColor: '#005eb8' as '#005eb8',
      padding: 10,
      borderRadius: 10,
    },
    imageContainer: {
      position: 'relative' as 'relative',
      width: '100%' as '100%',
      height: 200,
    },
    imageTitle: {
      position: 'absolute' as 'absolute',
      width: '100%' as '100%',
      textAlign: 'center' as 'center',
      top: '50%' as '50%',
      transform: [{ translateY: -20 }],
      fontSize: 40,
      fontWeight: 'bold' as 'bold',
      color: '#fff' as '#fff',
      shadowColor: 'black' as 'black',
      shadowOffset: { width: 0, height: 0 },
      shadowOpacity: 0.5,
      shadowRadius: 10,
    },
    pagination: {
      flexDirection: 'row' as 'row',
      justifyContent: 'center' as 'center',
      alignItems: 'center' as 'center',
    },
    pageItem: {
      margin: 5,
    },
    pageText: {
      fontSize: 16,
      color: '#005eb8' as '#005eb8',
    },
    toggleButton: {
      flexDirection: 'row' as 'row',
      alignItems: 'center' as 'center',
      justifyContent: 'space-between' as 'space-between',
      padding: 10,
      marginTop: 10,
      backgroundColor: '#005eb8' as '#005eb8',
      borderRadius: 10,
    },
    toggleLabel: {
      fontSize: 18,
      marginRight: 10,
      color: 'white' as 'white',
    },
    toggleIcon: {
      fontSize: 18,
      color: 'white' as 'white',
    },
    bold: {
      fontWeight: 'bold' as 'bold',
      color: mode === 'dark' ? '#fff' : '#212529',
    },
    item: {
      padding: 10,
      marginVertical: 5,
      marginHorizontal: 5,
      backgroundColor: mode === 'dark' ? '#444' : '#ddd',
      borderRadius: 10,
      flexDirection: 'row' as 'row',
      justifyContent: 'space-between' as 'space-between',
      alignItems: 'center' as 'center',
    },
    summary: {
      padding: 20,
      margin: 10,
      backgroundColor: mode === 'dark' ? '#212529' : '#fff',
      borderRadius: 10,
      shadowColor: '#212529' as '#212529',
      shadowOffset: { width: 0, height: 2 },
      shadowOpacity: 0.1,
      shadowRadius: 1.5,
      elevation: 3,
    },
    summaryTitle: {
      fontSize: 20,
      fontWeight: 'bold' as 'bold',
      color: mode === 'dark' ? '#fff' : '#212529',
      marginBottom: 10,
    },
    cartContainer: {
      flex: 1,
      padding: 0,
      alignItems: 'stretch' as 'stretch',
    },
    cartHeader: {
      fontSize: 24,
      fontWeight: 'bold' as 'bold',
      color: mode === 'dark' ? '#fff' : '#212529',
    },
    cartListItem: {
      flexDirection: 'row' as 'row',
      alignItems: 'center' as 'center',
      justifyContent: 'space-between' as 'space-between',
      padding: 10,
      borderBottomWidth: 1,
      borderBottomColor: '#cccccc' as '#cccccc',
    },
    cartImage: {
      width: 50,
      height: 50,
    },
    table: {
      width: '100%' as '100%',
      alignItems: 'stretch' as 'stretch',
    },
    tableRow: {
      flexDirection: 'row' as 'row',
      justifyContent: 'space-between' as 'space-between',
      padding: 10,
      borderBottomWidth: 1,
      borderBottomColor: '#cccccc' as '#cccccc',
      alignItems: 'center' as 'center',
    },
    tableCell: {
      flex: 1,
      justifyContent: 'center' as 'center',
      alignItems: 'center' as 'center',
      padding: 5,
      borderWidth: 1,
      borderColor: '#cccccc' as '#cccccc',
      borderStyle: 'solid' as 'solid',
    },
    quantityControls: {
      flexDirection: 'row' as 'row',
      alignItems: 'center' as 'center',
      justifyContent: 'center' as 'center',
    },
    icon: {
      color: mode === 'dark' ? '#fff' : '#212529',
    },
    cartText: {
      color: mode === 'dark' ? '#fff' : '#212529',
    },
    scrollView: {
      width: '100%' as '100%',
    },
    dataTable: {
      width: '100%' as '100%',
    },
    buttonContainer: {
      flexDirection: 'row' as 'row',
      justifyContent: 'space-between' as 'space-between',
      padding: 10,
    },
    filterSection: {
      marginHorizontal: 10,
      paddingVertical: 10,
    },
    list: {
      flexDirection: 'row' as 'row',
      flexWrap: 'wrap' as 'wrap',
      justifyContent: 'center' as 'center',
    },
    tableHeaderText: {
      fontWeight: 'bold' as 'bold',
      fontSize: 16,
      color: mode === 'dark' ? '#fff' : '#212529',
      textAlign: 'center' as 'center',
    },
    totalPriceContainer: {
      padding: 10,
      margin: 10,
      backgroundColor: mode === 'dark' ? '#212529' : '#fff',
      borderRadius: 10,
      alignItems: 'center' as 'center',
    },
    totalPriceText: {
      fontSize: 18,
      fontWeight: 'bold' as 'bold',
      color: mode === 'dark' ? '#fff' : '#212529',
    },
    picker: {
      height: 50,
      width: '100%' as '100%',
      color: mode === 'dark' ? '#fff' : '#212529',
      backgroundColor: mode === 'dark' ? '#212529' : '#fff',
    },
    switchContainer: {
      flexDirection: 'row' as 'row',
      alignItems: 'center' as 'center' | 'flex-start',
      justifyContent: 'center' as 'center' | 'flex-start',
    },
    switch: {
      width: 100,
    },
    addressContainer: {
      padding: 10,
      margin: 10,
      backgroundColor: mode === 'dark' ? '#212529' : '#fff',
      borderRadius: 10,
    },
  };

  return { styles, mode };
}
